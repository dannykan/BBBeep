/**
 * License Plate Utilities - Re-export from shared package
 * 保持向後兼容，現有的 import 不需要更改
 */
export {
  formatLicensePlate,
  normalizeLicensePlate,
  displayLicensePlate,
  isValidLicensePlate,
} from '@bbbeeep/shared';
