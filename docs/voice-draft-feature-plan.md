# 語音草稿功能規劃文檔

## 一、功能概述

### 問題定義
駕駛在行車中遇到不愉快事件時：
- 無法安全操作手機輸入車牌和訊息
- 情緒是當下的（5-10 秒），過了就消退
- 車牌是複雜的英數組合，容易遺忘

### 解決方案
**語音草稿 + AI 輔助解析**
1. 開車中：一鍵錄音（零操作負擔）
2. 稍後：AI 預處理好車牌和內容，用戶只需確認

---

## 二、用戶流程設計

### 2.1 快速錄音流程（開車中）

```
┌─────────────────────────────────────────────────────────────┐
│                        首頁 (HomeScreen)                     │
│                                                             │
│    ┌─────────────────────────────────────────────────┐     │
│    │                                                 │     │
│    │              （原有首頁內容）                    │     │
│    │                                                 │     │
│    └─────────────────────────────────────────────────┘     │
│                                                             │
│                                    ┌──────────────────┐    │
│                                    │  🎤 快速錄音     │    │
│                                    │  (浮動按鈕 FAB)  │    │
│                                    └──────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 點擊
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     快速錄音 Modal                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │                    ◉ 錄音中                         │   │
│  │                    00:05                            │   │
│  │                                                     │   │
│  │           「前面白色 Camry ABC 1234                 │   │
│  │             在中山路口亂切車道...」                  │   │
│  │                                                     │   │
│  │     ┌─────────┐              ┌─────────┐           │   │
│  │     │  取消   │              │ ✓ 儲存  │           │   │
│  │     └─────────┘              └─────────┘           │   │
│  │                                                     │   │
│  │   💡 說出車牌、車型、顏色、發生什麼事              │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 儲存
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│     ✅ 已儲存草稿                                           │
│     稍後會提醒你處理                                        │
│                                                             │
│     [查看草稿]  [繼續]                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 草稿列表頁面

```
┌─────────────────────────────────────────────────────────────┐
│  ←  語音草稿                                    編輯        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   💡 草稿會在 24 小時後自動刪除                             │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🔴 草稿 1                              30 分鐘前   │   │
│  │  ──────────────────────────────────────────────────  │   │
│  │  🚗 車牌候選：                                      │   │
│  │     [ABC-1234] [ABC-1235] [輸入其他]               │   │
│  │                                                     │   │
│  │  📍 白色 Toyota Camry                              │   │
│  │  ⚠️ 事件：危險切換車道                             │   │
│  │                                                     │   │
│  │  🔊 ▶ 00:00 ─────●───────── 00:12                 │   │
│  │                                                     │   │
│  │  AI 建議訊息：                                      │   │
│  │  「您好，剛才在中山路口的切換車道方式較為突然...」  │   │
│  │                                                     │   │
│  │  ┌──────────────┐  ┌──────────────┐               │   │
│  │  │   🗑️ 刪除   │  │  ✉️ 發送提醒 │               │   │
│  │  └──────────────┘  └──────────────┘               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🔴 草稿 2                               2 小時前   │   │
│  │  ──────────────────────────────────────────────────  │   │
│  │  🚗 車牌候選：                                      │   │
│  │     [DEF-5678] [輸入其他]                          │   │
│  │                                                     │   │
│  │  📍 黑色轎車                                       │   │
│  │  😊 事件：禮讓行人                                 │   │
│  │  ...                                               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 草稿轉發送流程

```
用戶點擊「發送提醒」
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│                     確認車牌                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │  AI 識別的車牌：                                    │   │
│  │                                                     │   │
│  │     ┌─────────────┐  ┌─────────────┐               │   │
│  │     │ ABC-1234 ✓ │  │  ABC-1235   │               │   │
│  │     └─────────────┘  └─────────────┘               │   │
│  │                                                     │   │
│  │     ┌─────────────────────────────────┐            │   │
│  │     │  其他：[          ]             │            │   │
│  │     └─────────────────────────────────┘            │   │
│  │                                                     │   │
│  │  🔊 重聽錄音確認                                   │   │
│  │     ▶ 「...ABC 一二三四...」                       │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│           [取消]              [下一步]                      │
└─────────────────────────────────────────────────────────────┘
        │
        │ 選擇車牌後
        ▼
┌─────────────────────────────────────────────────────────────┐
│                     選擇車輛類型                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │  這是什麼車輛？                                     │   │
│  │                                                     │   │
│  │     ┌─────────────┐  ┌─────────────┐               │   │
│  │     │   🚗 汽車   │  │   🛵 機車   │               │   │
│  │     └─────────────┘  └─────────────┘               │   │
│  │                                                     │   │
│  │  AI 猜測：汽車（白色 Toyota Camry）                │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
        │
        │ 選擇車型後
        ▼
  跳轉到現有的 CategoryScreen（Step 3）
  並預填 AI 建議的訊息內容
```

---

## 三、技術架構

### 3.1 資料庫 Schema 更新

```prisma
// 新增到 prisma/schema.prisma

model VoiceDraft {
  id              String    @id @default(cuid())
  userId          String
  user            User      @relation(fields: [userId], references: [id])

  // 語音檔案
  voiceUrl        String    // R2 URL
  voiceDuration   Int       // 秒數
  transcript      String?   // 語音轉文字結果

  // AI 解析結果
  parsedPlates    String[]  // 候選車牌 ["ABC-1234", "ABC-1235"]
  parsedVehicle   Json?     // { type: "car", color: "白色", brand: "Toyota", model: "Camry" }
  parsedEvent     Json?     // { type: "dangerous_driving", description: "危險切換車道" }
  suggestedMessage String?  // AI 建議的訊息

  // 位置（錄音時自動記錄）
  latitude        Float?
  longitude       Float?
  address         String?

  // 狀態
  status          DraftStatus @default(PENDING)
  expiresAt       DateTime   // 24小時後過期

  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}

enum DraftStatus {
  PENDING     // 等待處理
  PROCESSING  // AI 處理中
  READY       // 處理完成
  SENT        // 已發送
  EXPIRED     // 已過期
  DELETED     // 已刪除
}
```

### 3.2 API 設計

#### 3.2.1 建立語音草稿

```typescript
// POST /api/drafts/voice

// Request
{
  voiceUri: string;      // 本地檔案 URI（會上傳）
  duration: number;      // 秒數
  latitude?: number;
  longitude?: number;
}

// Response
{
  id: string;
  status: "PROCESSING";
  message: "草稿已儲存，AI 正在分析中...";
}
```

#### 3.2.2 取得草稿列表

```typescript
// GET /api/drafts

// Response
{
  drafts: [
    {
      id: string;
      voiceUrl: string;
      voiceDuration: number;
      transcript: string;
      parsedPlates: string[];
      parsedVehicle: {
        type: "car" | "scooter";
        color: string;
        brand: string;
        model: string;
      };
      parsedEvent: {
        type: string;
        description: string;
        category: ReminderCategory;  // 對應到現有分類
      };
      suggestedMessage: string;
      status: DraftStatus;
      createdAt: string;
      expiresAt: string;
    }
  ];
  count: number;
}
```

#### 3.2.3 從草稿發送訊息

```typescript
// POST /api/drafts/:id/send

// Request
{
  selectedPlate: string;      // 用戶確認的車牌
  vehicleType: "car" | "scooter";
  category: ReminderCategory;
  situation?: string;
  customText?: string;
  useAiSuggestion: boolean;   // 是否使用 AI 建議訊息
  sendMode: "text" | "voice" | "ai";
}

// Response
{
  success: boolean;
  messageId: string;
  pointsUsed: number;
  remainingPoints: number;
}
```

### 3.3 AI 車牌解析服務

```typescript
// apps/api/src/ai/services/voice-parser.service.ts

import { Injectable } from '@nestjs/common';
import OpenAI from 'openai';

@Injectable()
export class VoiceParserService {
  constructor(private openai: OpenAI) {}

  async parseVoiceContent(transcript: string): Promise<ParsedVoiceContent> {
    const prompt = `
你是台灣車牌辨識助手。分析以下語音轉文字內容，提取關鍵資訊。

## 台灣車牌格式規則
- 汽車：3字母-4數字（如 ABC-1234）、2字母-4數字（如 AB-1234）、4數字-2字母（如 1234-AB）
- 機車：3字母-3數字（如 ABC-123）、3數字-3字母（如 123-ABC）

## 常見口語轉換
字母：
- 「阿/啊」= A、「逼/比」= B、「西/C」= C、「滴/低」= D
- 「伊」= E、「誒福」= F、「乩/雞」= G、「誒曲/H」= H
- 「愛」= I、「乩誒/J」= J、「K/ㄎㄟ」= K、「誒偶/L」= L
- 「誒母/M」= M、「誒恩/N」= N、「歐」= O、「批/P」= P
- 「乞/Q」= Q、「阿/R」= R、「誒死/S」= S、「踢/T」= T
- 「優/U」= U、「V/威」= V、「搭波優/W」= W、「誒克斯/X」= X
- 「外/歪/Y」= Y、「誒/Z」= Z

數字：
- 「么/腰」= 1、「兩/二」= 2、「三」= 3、「四/肆」= 4
- 「五/伍」= 5、「六/陸」= 6、「七/柒/拐」= 7、「八/捌」= 8
- 「九/狗/勾」= 9、「洞/零」= 0

## 輸出格式（JSON）
{
  "plates": ["ABC-1234", "ABC-1235"],  // 1-3 個最可能的車牌，信心度由高到低
  "plateConfidence": 0.85,              // 0-1，對車牌解析的信心度
  "vehicle": {
    "type": "car",                      // "car" 或 "scooter"，根據車牌格式和描述判斷
    "color": "白色",
    "brand": "Toyota",
    "model": "Camry"
  },
  "event": {
    "category": "SAFETY_REMINDER",      // VEHICLE_REMINDER, SAFETY_REMINDER, PRAISE, OTHER
    "type": "dangerous_lane_change",
    "description": "危險切換車道",
    "location": "中山路口",
    "sentiment": "negative"             // positive, negative, neutral
  },
  "suggestedMessage": "您好，剛才在中山路口的切換車道方式較為突然，後方車輛需要緊急煞車。行車時請多留意周遭車輛，保持安全距離。祝行車平安！"
}

## 注意事項
1. 如果無法確定車牌，提供 2-3 個可能的候選
2. 如果完全無法識別車牌，plates 返回空陣列
3. suggestedMessage 要禮貌、客觀、不帶攻擊性
4. 如果是正面事件（如禮讓），category 應為 PRAISE

語音內容：
"""
${transcript}
"""
`;

    const response = await this.openai.chat.completions.create({
      model: 'gpt-4o-mini',
      messages: [{ role: 'user', content: prompt }],
      response_format: { type: 'json_object' },
      temperature: 0.3,  // 低溫度提高一致性
    });

    return JSON.parse(response.choices[0].message.content);
  }
}
```

### 3.4 前端組件結構

```
apps/mobile/src/
├── screens/
│   ├── drafts/
│   │   ├── DraftsScreen.tsx          // 草稿列表頁
│   │   ├── DraftDetailScreen.tsx     // 草稿詳情（確認車牌）
│   │   └── components/
│   │       ├── DraftCard.tsx         // 草稿卡片
│   │       ├── PlateSelector.tsx     // 車牌選擇器
│   │       └── VoicePlayer.tsx       // 語音播放器（複用現有）
│   └── main/
│       └── HomeScreen.tsx            // 新增 FAB 按鈕
├── components/
│   └── QuickRecordModal.tsx          // 快速錄音 Modal
├── context/
│   └── DraftContext.tsx              // 草稿狀態管理
└── navigation/
    └── DraftsNavigator.tsx           // 草稿相關頁面導航
```

---

## 四、實作計畫

### Phase 1: 基礎架構（後端）

| 任務 | 說明 | 預估 |
|------|------|------|
| 1.1 | 新增 VoiceDraft Prisma Schema | - |
| 1.2 | 建立 drafts module（CRUD API） | - |
| 1.3 | 實作 VoiceParserService（AI 解析） | - |
| 1.4 | 新增草稿過期清理 Cron Job | - |
| 1.5 | 整合語音上傳到 R2 | - |

### Phase 2: 快速錄音（前端）

| 任務 | 說明 | 預估 |
|------|------|------|
| 2.1 | 建立 QuickRecordModal 組件 | - |
| 2.2 | 首頁新增 FAB 按鈕 | - |
| 2.3 | 實作錄音 + 自動儲存草稿 | - |
| 2.4 | 錄音時自動取得 GPS 位置 | - |

### Phase 3: 草稿管理（前端）

| 任務 | 說明 | 預估 |
|------|------|------|
| 3.1 | 建立 DraftsScreen 列表頁 | - |
| 3.2 | 建立 DraftCard 組件 | - |
| 3.3 | 實作車牌選擇器 PlateSelector | - |
| 3.4 | 串接草稿 → 發送流程 | - |

### Phase 4: 通知與優化

| 任務 | 說明 | 預估 |
|------|------|------|
| 4.1 | 推播提醒處理草稿（30分鐘後） | - |
| 4.2 | iOS Widget 快速錄音 | - |
| 4.3 | 草稿即將過期提醒 | - |
| 4.4 | 車牌解析準確度優化 | - |

---

## 五、UI 元件規格

### 5.1 快速錄音 FAB

```typescript
// 樣式規格
{
  position: 'absolute',
  bottom: 100,  // 在 Tab Bar 上方
  right: 20,
  width: 64,
  height: 64,
  borderRadius: 32,
  backgroundColor: '#4A6FA5',  // Primary color
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 4 },
  shadowOpacity: 0.3,
  shadowRadius: 8,
  elevation: 8,
}

// 圖示：麥克風 icon
// 長按開始錄音，再次點擊結束
// 錄音中顯示脈動動畫
```

### 5.2 草稿卡片

```typescript
// 資訊層級
1. 錄音時間（相對時間：30分鐘前）
2. 車牌候選（Chip 形式，可點擊選擇）
3. 車輛描述（顏色 + 車型）
4. 事件類型（icon + 文字）
5. 語音播放器（縮小版）
6. AI 建議訊息（可展開）
7. 操作按鈕：刪除 / 發送

// 狀態顏色
- PROCESSING: 灰色，顯示 loading
- READY: 正常顏色
- 即將過期（< 2小時）: 橘色警告
```

### 5.3 車牌選擇器

```typescript
// 設計
- 水平排列的 Chip
- 可選擇或輸入自訂
- 選中狀態：Primary color 填充
- 「其他」選項開啟輸入框
- 輸入框帶有車牌格式化（自動加 -）
```

---

## 六、資料流圖

```
┌─────────┐     ┌─────────────┐     ┌──────────────┐
│  用戶   │────▶│  快速錄音   │────▶│  上傳語音    │
│  點擊   │     │   Modal     │     │  到 R2      │
└─────────┘     └─────────────┘     └──────────────┘
                                           │
                                           ▼
┌─────────┐     ┌─────────────┐     ┌──────────────┐
│  草稿   │◀────│   建立      │◀────│   Whisper    │
│  列表   │     │   草稿      │     │   轉文字     │
└─────────┘     └─────────────┘     └──────────────┘
     │                                     │
     │                                     ▼
     │                              ┌──────────────┐
     │                              │   GPT-4o    │
     │                              │   解析內容   │
     │                              └──────────────┘
     │                                     │
     ▼                                     ▼
┌─────────┐     ┌─────────────┐     ┌──────────────┐
│  用戶   │────▶│  確認車牌   │◀────│   更新草稿   │
│  選擇   │     │  + 車型     │     │   狀態       │
└─────────┘     └─────────────┘     └──────────────┘
                      │
                      ▼
              ┌─────────────┐
              │  跳轉到現有  │
              │  發送流程    │
              │ (Step 3+)   │
              └─────────────┘
```

---

## 七、風險與對策

| 風險 | 影響 | 對策 |
|------|------|------|
| 車牌辨識準確度不足 | 用戶體驗差 | 提供多個候選 + 手動輸入 |
| 語音轉文字失敗 | 無法解析 | 保留原始音檔，用戶可重聽 |
| AI 解析成本過高 | 成本增加 | 使用 gpt-4o-mini，控制 prompt 長度 |
| 用戶忘記處理草稿 | 功能閒置 | 30分鐘推播 + 24小時過期 |
| 錄音檔案過大 | 上傳慢 | 限制 30 秒，壓縮音質 |

---

## 八、成功指標

| 指標 | 目標 |
|------|------|
| 快速錄音使用率 | > 30% 用戶使用過 |
| 草稿轉發送率 | > 50% 草稿最終發送 |
| 車牌首選準確率 | > 70% 用戶選擇第一個候選 |
| 發送流程縮短 | 從草稿發送比傳統流程快 50% |

---

## 九、後續擴展

### v1.1
- iOS Widget 一鍵錄音
- Siri Shortcuts 整合

### v1.2
- Apple Watch 快速錄音
- Android 快捷方式

### v2.0
- CarPlay / Android Auto 整合
- 行車記錄器影片匯入 + OCR 車牌辨識
